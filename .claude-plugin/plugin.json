{
  "name": "whatsapp-mcp",
  "version": "1.0.0",
  "description": "Search and send WhatsApp messages from Claude — Go bridge + MCP server with media, groups and opt-in audio transcription",
  "author": { "name": "Rodrigo Gonçalves" },
  "mcpServers": {
    "whatsapp": {
      "command": "uv",
      "args": ["--directory", "${CLAUDE_PLUGIN_ROOT}/whatsapp-mcp-server", "run", "main.py"]
    }
  }
}
